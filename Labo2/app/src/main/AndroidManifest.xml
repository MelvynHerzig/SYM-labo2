<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.heigvd.iict.sym.labo2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Labo2"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".manipulations.GraphqlActivity"
            android:exported="true" />
        <activity
            android:name=".manipulations.CompressedActivity"
            android:exported="true" />
        <activity
            android:name=".manipulations.SerializedActivity"
            android:exported="true" />
        <activity
            android:name=".manipulations.DelayedActivity"
            android:exported="true" />
        <activity
            android:name=".manipulations.AsyncActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>